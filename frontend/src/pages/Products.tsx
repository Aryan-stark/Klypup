/**
 * Products.tsx — Product catalog with filter, sort, search.
 *
 * Shows: SKU, name, category, current price, margin %, stock, last recommendation status.
 * Filters: category dropdown, search input, sort by name/price/margin/stock.
 * TODO: implement using useProducts() hook + ProductTable + ProductFilters components
 */
export default function Products() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold">Product Catalog</h1>
      <p className="text-muted-foreground">TODO: product table with filters</p>
    </div>
  )
}
